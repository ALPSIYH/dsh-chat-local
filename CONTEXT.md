# 群组协作领域词汇

本文件定义 `dsh-chat-local` 的核心概念，供阅读代码、界面文案和文档时对齐语义。

一句话前提：**群组复用团队配置，对话隔离工作上下文，执行回合不是任务完成的判据。**

## 词汇

**群组**
稳定的协作团队，持有成员、职责和供未来对话使用的默认配置。
_Avoid_：把群组与一段无限延伸的聊天混为一谈。

**对话**
群组内围绕一个主题开展工作的独立上下文，拥有自己的消息、台账、材料和执行记录。
_Avoid_：用「回合」表示新主题。

**执行回合**
一次有边界的成员唤醒与回复过程；结束不表示任务完成。
_Avoid_：任务、对话。

**分支对话**
显式引用选定来源的新对话，不继承来源的活动任务或授权。
_Avoid_：克隆任务、继续执行。

**Agent 名册**
稳定身份与版本化的名称、职务、职责、模型偏好。可在多个群组引用；不携带目录或权限；人格和本人观察归属于 Agent，原生 Session 上下文由 DSH 管理。收存只隐藏名册条目，保留群组与历史引用。

**群组成员池**
一个群组完整的 Agent 引用与局部配置快照，与新对话的预选名单分开。新增成员不自动扩张预选；对话临时加人不反向改动群组。

**本次参与者**
只属于一段对话的称呼、职务、模型与 Session 绑定。新话题默认独立上下文；历史台账责任以稳定的 participation 记录保留，不因移出名单而失联。

**DSH 会话**
实际承载 Agent 执行与模型上下文的原生实体；新对话为成员分配不同会话。

**草稿**
有独立 ID 与修订号的未开始讨论或群组配置；暂存不建立原生会话。首发以持久化的开始快照关联对话、首条消息及派送意图。结果不明时不自动重播。

**常用阵容**
可保存多套名称、职责和模型组合；选择后形成当前草稿快照。阵容不携带工作目录、执行权限、Agent preset、私聊历史或旧台账。

**工作环境绑定**
本次的目录、逐成员目录与原生 Agent preset；与可复用阵容分离。执行模式的确认绑定到不可变的开始快照，不是通用授权。共享同一目录仍是同一份文件，并非隔离副本。

**原生会话共享例外**
连接已有 Session 时，模型修改会影响该原生会话及引用它的其他对话；显示影响范围并要求确认。只有新建独立 Session 才有默认的「本对话配置隔离」。

**整理请求**
固定条目 ID、修订号、依赖关系及动作的预览批次。Agent 可提议，用户确认后执行；并发变化的条目跳过并返回回执，不因整理而唤醒全体成员。

**终止 / 归档 / 恢复显示 / 重新开工**
终止为 cancelled，不等于验收；归档只移出视图；恢复显示保留原完成结果，未完成的历史保持 paused；重新开工才创建需重新认领与验收的工作周期。取消过时依赖必须明确指定，归档前置不等于满足前置。

## 当前边界

- 新建群组从空白开始；打开页面本身不制造持久草稿，输入后才保存。纯组队或名册保存不建立 Session、不执行模型。新建 Agent 随父流程一起完成才发布，取消不留下孤儿。
- 名册、群组、对话草稿与既有对话共用同一套选人、编辑和目录组件。原生导入配置与接续历史明确分开：接续只能进具体对话，且须核对忙碌状态、共享引用、配置与权限作用域。
- 收存群组需先处理执行中或待派送的工作，并明确暂停未来监测。恢复只恢复显示，不自动重启工作；未知的提交结果保留原凭据，不盲目重发。
- 跨对话任务转交、安全回合节点自动换人、群组级共同材料尚未实现。不能通过复制阵容假装完成这些流程。
- 保存到常用阵容采用另存语义：新快照不会反向更新原群组预设或历史对话。
- 房间状态格式与插件版本绑定，旧版程序无法读取新格式。升级前请备份；备份只保存升级时点，直接覆盖旧备份会丢失之后新增的数据，当前没有无损降级工具。

## 持續身分與記憶

**Agent 身分**：跨群組、任務與執行會話延續的同一位參與者。改名、換模型或換工作場所不會自動產生另一位 Agent。
_Avoid_: 把 Session、顯示名稱或房間職務當成一個人。

**人格**：由使用者設定的持續性性格、價值取向、溝通偏好與工作習慣。不等同於當前任務，也不是從幾次成功或失敗推定的人格評分。

**參與**：一位 Agent 在一段具體對話中承擔的角色與執行身分。同一 Agent 可以有多段參與。

**房間**：一段有獨立消息、任務、章程與權限的協作對話。

**群組**：可重用的成員與工作設定；不表示每個成員都看過該群組所有對話。

**本人觀察**：某個內容確實被交給該 Agent，或是該 Agent 本人產生的內容。參與者名單本身不能證明已看過每一則消息。

**個人記憶**：一位 Agent 可以跨工作回想的本人觀察與本人判斷，並保留來源。回想不會重啟旧任務，也不授予舊工作中的權限。

**主觀評價**：某位 Agent 對另一位 Agent 的可撤回判斷。引用證據代表判斷有出處，不代表判斷已被證實。

**房間行為紀錄**：本房間內投遞、發言、審閱與分歧等可重算的計數。它不等同於某位 Agent 的私有信任程度。

**Episode**：一條根消息所啟動的整段協作，包括其重試與後續回應。

**Run**：實驗條件固定的一段記錄。切出新的 Run 不會自動使它與之前的 Run 統計獨立。

**記憶注入重置臂**：本 Episode 的插件記憶覆蓋層不使用重置點以前的計數、本人評價與跨工作回想。人格、共享對話和 DSH 原生會話上下文保留，重新看見的內容可再次形成記憶；不能稱為完全失憶條件。
