# DSH 群组协作

群组复用团队配置，对话隔离工作上下文；执行回合不是任务完成的判据。

## Language

**群组**：稳定的协作团队，持有成员、职责和供未来对话使用的默认配置。
_Avoid_: 把群组与一段无限延伸的聊天混为一谈

**对话**：群组内围绕一个主题开展工作的独立上下文，拥有自己的消息、台账、材料和执行记录。
_Avoid_: 用“回合”表示新主题

**执行回合**：一次有边界的成员唤醒与回复过程；结束不表示任务完成。
_Avoid_: 任务、对话

**分支对话**：显式引用选定来源的新对话，不继承来源的活动任务或授权。
_Avoid_: 克隆任务、继续执行

**Agent 名冊**：穩定身分與版本化的名稱、職務、職責、模型偏好。可在多個群組引用；不攜帶目錄、權限或 Session 歷史。收存只隱藏名冊項目，保留群組與歷史引用。

**群組成員池**：一個群組完整的 Agent 引用與局部配置快照，與新對話的預選名單分開。新增成員不自動擴張預選；對話臨時加人不反向改群組。

**本次參與者**：只屬於一段對話的稱呼、職務、模型與 Session 綁定。新話題預設獨立上下文；歷史台帳責任以穩定 participation 記錄保留，不能因移出名單而失聯。

**DSH 会话**：实际承载 Agent 执行与模型上下文的原生实体；新对话为成员分配不同会话。

**草稿**：有独立 ID 与修订号的未开始讨论或群组配置；暂存不建立原生会话。首发以持久化开始快照关联对话、首条消息及派送意图。结果不明不自动重播。

**常用阵容**：可保存多套名称、职责和模型组合；选择后形成当前草稿快照。阵容不携带工作目录、执行权限、Agent preset、私聊历史或旧台账。

**工作环境绑定**：本次的目录、逐成员目录与原生 Agent preset；与可复用阵容分离。执行模式的确认绑定到不可变开始快照，不是通用授权。共享同一目录仍是同一份文件，并非隔离副本。

**原生会话共享例外**：连接已有 Session 时，模型修改会影响原生会话及引用它的其他对话；显示影响范围并确认。新建独立 Session 才有默认的本对话配置隔离。

**整理请求**：固定条目 ID、修订号、依赖关系及动作的预览批次。Agent 可提议，用户确认执行；并发变化的条目跳过并返回回执，不因整理唤醒全体成员。

**终止／归档／恢复显示／重新开工**：终止为 cancelled，不等于验收；归档只移出视图；恢复显示保留原完成结果，未完历史保持 paused；重新开工才创建需重新认领与验收的工作周期。取消过时依赖必须明确指定，归档前置不等于满足前置。

## Current boundaries

- v14 追加 Agent 身分、版本、participation 與作用域限定的原生接續預覽憑據；舊房間 ID、消息、台帳、Session 綁定、草稿與分享範圍不改寫。同名不合併；舊程式不能讀取 v14，資料備份不是有新工作後的無損降級路徑。
- 新建群組從空白開始；開頁本身不製造持久草稿，輸入後才保存。純組隊／名冊保存不建立 Session、不執行模型。新建 Agent 隨父流程完成才發布；取消不留下孤兒。
- 名冊、群組、對話草稿與既有對話共用選人／編輯／目錄元件。原生匯入配置與接續歷史明確分開；接續只能進具體對話，須核對 busy、共享引用、配置與權限作用域。
- 收存群組需先處理執行／待派送工作，明確暫停未來監測。恢復只恢復顯示，不自動重啟工作。未知提交結果保留原憑據，不盲目重發。
- 当前分支仍采用既有选定原消息入口；跨对话任务转交、安全回合节点自动换人、群组共同材料尚未实现，不能通过复制阵容假装完成这些流程。
- 当前保存到常用阵容采用另存；新快照不反向更新原群组预设或历史对话。
